#include<stdio.h>
int main(){
    // FILE* ptr=fopen("jaiveer.txt","r"); // opening the txt file in reading mode
    // char str[100];
    // while (fgets(str,100,ptr)!= NULL){
    //     printf("%s\n",str);
    // }
    FILE* ptr=fopen("lesgo.txt","w");
    return 0;
} 